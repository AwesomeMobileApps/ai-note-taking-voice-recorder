module.exports = {
  dependencies: {
    'react-native-voice': {
      platforms: {
        ios: null, // Use the podspec directly
      },
    },
    'react-native-vector-icons': {
      platforms: {
        ios: null, // Use the podspec directly
      },
    },
    'react-native-fs': {
      platforms: {
        ios: null, // Use the podspec directly
      },
    },
  },
}; 